import { LowerCaseUrlSerializer } from './lowe-case-url-serializer';

describe('LoweCaseUrlSerializer', () => {
  it('should create an instance', () => {
    expect(new LowerCaseUrlSerializer()).toBeTruthy();
  });
});
